# ADR-003：版本化可移植逻辑备份

- 状态：已接受
- 日期：2026-07-21

## 背景

直接复制 SQLite 文件实现简单，但与 Room schema、WAL 状态和应用版本强耦合，不利于跨版本恢复与审计。

## 决策

`.pbk` 使用 ZIP 容器、manifest、稳定 JSON DTO、格式版本和 SHA-256。恢复先做结构与语义校验，再在 Room 事务中全量替换。

## 后果

格式可读、可测试且独立于数据库迁移，但需要维护 DTO、migrator 和黄金 fixture。v1 不加密，用户需选择可信保存位置；加密另立需求和 ADR。
