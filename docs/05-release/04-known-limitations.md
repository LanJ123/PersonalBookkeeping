# 1.0.0-rc1 已知限制

- 仅支持单机、单用户、人民币记账；没有账号、云同步和多币种。
- 不支持自动读取短信、通知、银行或支付平台账单。
- CSV 只用于通用导出，完整恢复必须使用 `.pbk`。
- 忘记应用锁所依赖的系统认证时，应用内没有独立找回机制。
- 模拟器性能只用于趋势；正式 P95、TalkBack、系统锁屏和厂商 ROM 行为仍需目标真机验证。
- API 37 前向兼容为可选项，本轮因本机内存不足未执行。
- 当前 Release APK/AAB 未使用正式生产密钥签名，不能作为正式分发物。
